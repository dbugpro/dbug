{
  "spec": "v260303.1",
  "universal_core_identity": "dbug.",
  "admin_core_identity": "admin.",
  "trailing_period_enforced": true,
  "validation_rules": {
    "dbug_literal": "dbug.",
    "admin_literal": "admin.",
    "assertion": "registry['universal_core_identity'] == 'dbug.' && registry['admin_core_identity'] == 'admin.'"
  },
  "critical_constraint": "Trailing period is non-negotiable — 'dbug' ≠ 'dbug.' | 'admin' ≠ 'admin.'"
}
